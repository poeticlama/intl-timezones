{
  "name": "intl-timezones",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "bunx --bun @biomejs/biome lint ./src",
    "lint:fix": "bunx --bun @biomejs/biome lint --write ./src",
    "format": "bunx --bun @biomejs/biome format --write ./src",
    "check": "bunx --bun @biomejs/biome check --write ./src"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  }
}
